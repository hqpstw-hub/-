#!/data/data/com.termux/files/usr/bin/sh

# 強制匯入 Termux 的 Python 路徑，確保抓得到套件
export PYTHONPATH='/data/data/com.termux/files/usr/lib/python3.12/site-packages'
PYTHON='/data/data/com.termux/files/usr/bin/python3'
CURL='/data/data/com.termux/files/usr/bin/curl'

URL_LIST='https://raw.githubusercontent.com/hqpstw-hub/-/refs/heads/main/twTVlist.txt'
SRC='/data/data/com.termux/files/home/twTVlist.txt'
DEST='/sdcard/Download/twTV.m3u'

echo "🌐 [STEP 1] 下載 GitHub 清單..."
$CURL -L -k -s "$URL_LIST" -o "$SRC"
sed -i 's/\r//g' "$SRC"

echo "#EXTM3U" > "$DEST"
echo "🚀 [STEP 2] 解析中..."

while read -r line; do
    [ -z "$line" ] && continue
    LINK=$(echo "$line" | grep -o "https://www.youtube.com/[^ ]*")
    NAME=$(echo "$line" | cut -d',' -f1)

    if [ -n "$LINK" ]; then
        echo "📡 正在解析: $NAME"
        # 增加解析參數，模擬 iOS 客戶端繞過阻擋
        REAL=$($PYTHON -m yt_dlp --no-warnings --extractor-args "youtube:player-client=ios,web" -g -f b "$LINK" 2>/dev/null)
        
        if [ -n "$REAL" ]; then
            echo "#EXTINF:-1,$NAME" >> "$DEST"
            echo "$REAL" >> "$DEST"
            echo "   ✅ 成功"
        else
            echo "   ❌ 失敗"
        fi
    fi
done < "$SRC"
echo "✨ 完成！檔案在: $DEST"
